# Ad hoc plan files saved via `terraform plan -out=<name>` (not matched by the
# repo-wide *.tfplan pattern since these don't use a .tfplan extension)
tfplan*

# Local-only variable files that may contain secrets (e.g. db_master_password)
*.tfvars
*.tfvars.json
!*.tfvars.example
