---
- name: Provision app EC2 instance
  hosts: app
  become: true
  vars:
    docker_gpg_url: https://download.docker.com/linux/ubuntu/gpg
    docker_repo: "deb [arch=amd64] https://download.docker.com/linux/ubuntu {{ ansible_distribution_release }} stable"

  tasks:
    # ---------------------------------------------------------------
    # System basics
    # ---------------------------------------------------------------
    - name: Wait for apt locks to be released
      shell: while fuser /var/lib/dpkg/lock-frontend /var/lib/apt/lists/lock /var/lib/dpkg/lock >/dev/null 2>&1; do sleep 2; done
      changed_when: false

    - name: Update apt cache
      command: apt-get update
      changed_when: false

    - name: Upgrade apt packages
      command: apt-get dist-upgrade -y
      environment:
        DEBIAN_FRONTEND: noninteractive
      changed_when: false

    - name: Update apt cache (module)
      apt:
        update_cache: true
        cache_valid_time: 3600

    - name: Install system packages
      apt:
        name:
          - apt-transport-https
          - ca-certificates
          - curl
          - gnupg
          - software-properties-common
          - git
          - tmux
          - sqlite3
          - mysql-client
          - unzip
          - jq
        state: present

    # ---------------------------------------------------------------
    # Add Group and User
    # ---------------------------------------------------------------
    - name: Create prometheus system group
      group:
        name: prometheus
        system: true

    - name: Create prometheus system user
      user:
        name: prometheus
        group: prometheus
        shell: /sbin/nologin
        system: true
        create_home: false

    # ---------------------------------------------------------------
    # Make folder and set priviledge
    # ---------------------------------------------------------------
    - name: Create /var/lib/node directory with ownership and permissions
      file:
        path: /var/lib/node
        state: directory
        owner: prometheus
        group: prometheus
        mode: '0775'
        recurse: true

    # ---------------------------------------------------------------
    # Add Exporter
    # ---------------------------------------------------------------
    - name: Download node_exporter
      command: wget https://github.com/prometheus/node_exporter/releases/download/v1.11.1/node_exporter-1.11.1.linux-amd64.tar.gz
      args:
        chdir: /home/ubuntu

    - name: Extract node_exporter
      command: tar -xvf node_exporter-1.11.1.linux-amd64.tar.gz
      args:
        chdir: /home/ubuntu

    # ---------------------------------------------------------------
    # Set exporter config and start exporter
    # ---------------------------------------------------------------
    - name: Move node_exporter binary to /var/lib/node/
      copy:
        src: /home/ubuntu/node_exporter-1.11.1.linux-amd64/node_exporter
        dest: /var/lib/node/node_exporter
        mode: '0755'
        remote_src: true

    - name: Create node.service systemd unit file
      copy:
        dest: /etc/systemd/system/node.service
        content: |
          [Unit]
          Description=Prometheus Node Exporter
          Documentation=https://prometheus.io/docs/introduction/overview/
          Wants=network-online.target
          After=network-online.target

          [Service]
          Type=simple
          User=prometheus
          Group=prometheus
          ExecReload=/bin/kill -HUP $MAINPID
          ExecStart=/var/lib/node/node_exporter
          SyslogIdentifier=prometheus_node_exporter
          Restart=always

          [Install]
          WantedBy=multi-user.target

    - name: Enable and start node
      systemd:
        name: node
        state: started
        enabled: true
        daemon_reload: true
